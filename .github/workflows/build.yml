name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v1
      - name: Install lerna
        run: yarn global add lerna
      - name: Bootstrap
        run: yarn bootstrap
      - name: Build
        run: yarn run build
    

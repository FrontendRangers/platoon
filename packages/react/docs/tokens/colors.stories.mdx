import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks';
import { colors } from '../../src/presets/theme-platoon';

<Meta title="Tokens/Colors" />

# Colors

<!-- TODO: get from context -->

<ColorPalette>
    {Object.keys(colors).map(color => <ColorItem
        title={color}
        subtitle={`theme.color.${color}`}
        colors={[colors[color][100], colors[color][200], colors[color][300], colors[color][400], colors[color][500], colors[color][600], colors[color][700], colors[color][800]]}
    />)}
</ColorPalette>

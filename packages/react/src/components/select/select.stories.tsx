import React from 'react';
import { storiesOf } from '@storybook/react';
import { Select } from './';

storiesOf('Components|Forms.Select', module).add('default', () => (
    <Select>Select component</Select>
));

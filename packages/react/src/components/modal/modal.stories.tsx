import React from 'react';
import { Modal } from './';

export default {
    title: 'Components/Overlays/Modal',
};

export const Default = () => <Modal>I'm a modal</Modal>;

Default.story = {
    name: 'default',
};

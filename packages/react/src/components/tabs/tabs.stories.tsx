import React from 'react';
import { storiesOf } from '@storybook/react';
import { Tabs } from './';

storiesOf('Components|Others.Tabs', module).add('default', () => (
    <Tabs>Tabs component</Tabs>
));

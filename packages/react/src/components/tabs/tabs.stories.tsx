import React from 'react';
import { Tabs } from './';

export default {
    title: 'Components/Others/Tabs',
};

export const Default = () => <Tabs>Tabs component</Tabs>;

Default.story = {
    name: 'default',
};

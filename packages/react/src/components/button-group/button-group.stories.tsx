import React from 'react';
import { storiesOf } from '@storybook/react';
import ButtonGroup from './';

storiesOf('Components|Actions.Button Group', module).add('default', () => (
    <ButtonGroup>Button Group component</ButtonGroup>
));

import React from 'react';
import { Radio } from './';

export default {
    title: 'Components/Forms/Radio',
};

export const Default = () => <Radio></Radio>;

Default.story = {
    name: 'default',
};

import React from 'react';
import { FormElement } from './';

export default {
    title: 'Forms/FormElement',
    component: FormElement,
};

export const Default = () => <FormElement>Form Element component</FormElement>;

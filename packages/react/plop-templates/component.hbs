import React from 'react';
import { Box, BoxProps } from '../../primitives/box';

export type {{properCase name}}Props = BoxProps;

type Props = {{properCase name}}Props;

const {{properCase name}}: React.FC<Props> = ({
    children,
    ...props
    }) => <Box {...props}>{children}</Box>;

export default {{properCase name}};

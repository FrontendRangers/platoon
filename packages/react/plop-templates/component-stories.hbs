import React from 'react';
import { storiesOf } from '@storybook/react';
import {{properCase name}} from './';

storiesOf('Components|{{titleCase name}}', module).add('default', () => (
<{{properCase name}}>{{titleCase name}} component</{{properCase name}}>
));

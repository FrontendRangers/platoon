import system from './system';

export const position = system({
    prop: 'position',
});

export const zIndex = system({
    prop: 'zIndex',
    themeKey: 'zIndexes',
});

export const top = system({
    prop: 'top',
});

export const right = system({
    prop: 'right',
});

export const bottom = system({
    prop: 'bottom',
});

export const left = system({
    prop: 'left',
});

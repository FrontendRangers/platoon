import { css } from 'styled-components';

const ButtonStyles = css`
    display: inline-flex;
    flex-wrap: nowrap;
    justify-content: center;
    align-content: center;
    align-items: center;
    position: relative;
`;

export { ButtonStyles };
